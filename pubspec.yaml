name: tbib_loading_transition_button_and_social
description: A Flutter plugin for generating sign in buttons for different social media accounts And ustomizable transition button for Flutter. Animate you loading proccess with ease.
version: 0.0.1
homepage: https://github.com/the-best-is-best/tbib_loading_transition_button_and_social

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  vector_math: ^2.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

 assets:
     - images/
